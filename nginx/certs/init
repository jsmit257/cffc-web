#!/bin/sh

openssl req -x509 \
  -newkey rsa:4096 \
  -keyout key.pem \
  -out cert.pem \
  -sha256 \
  -days 3650 \
  -nodes \
  -subj "/C=US/ST=Colorado/L=Denver/O=huautla/OU=mycology/CN=localhost" \
  -addext "subjectAltName=DNS:10.0.0.4,IP:10.0.0.4"
